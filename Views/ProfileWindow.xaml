<Window x:Class="StudyPlanner.Views.ProfileWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="Profil ve Ayarlar" Height="500" Width="400"
        WindowStartupLocation="CenterScreen"
        Background="{DynamicResource SurfaceBrush}">

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/> <!-- Header -->
            <RowDefinition Height="*"/>    <!-- Content -->
            <RowDefinition Height="Auto"/> <!-- Footer -->
        </Grid.RowDefinitions>

        <!-- Header -->
        <TextBlock Grid.Row="0" 
                   Text="👤 Profil Ayarları" 
                   FontSize="24" 
                   FontWeight="Bold" 
                   Foreground="{DynamicResource TextPrimaryBrush}"
                   Margin="0,0,0,20"/>

        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
            <StackPanel>
                <!-- Personal Info -->
                <GroupBox Header="Kişisel Bilgiler" 
                          Foreground="{DynamicResource TextPrimaryBrush}"
                          BorderBrush="{DynamicResource BorderBrush}"
                          Padding="10" 
                          Margin="0,0,0,15">
                    <StackPanel>
                        <Label Content="Kullanıcı Adı" Foreground="{DynamicResource TextPrimaryBrush}"/>
                        <TextBox Text="{Binding Username}" IsReadOnly="True" Opacity="0.7"/>
                        
                        <Label Content="Ad Soyad" Foreground="{DynamicResource TextPrimaryBrush}" Margin="0,10,0,0"/>
                        <TextBox Text="{Binding FullName}"/>
                        
                        <Label Content="E-posta" Foreground="{DynamicResource TextPrimaryBrush}" Margin="0,10,0,0"/>
                        <TextBox Text="{Binding Email}"/>
                    </StackPanel>
                </GroupBox>

                <!-- UI Settings -->
                <GroupBox Header="Görünüm Ayarları" 
                          Foreground="{DynamicResource TextPrimaryBrush}"
                          BorderBrush="{DynamicResource BorderBrush}"
                          Padding="10" 
                          Margin="0,0,0,15">
                    <StackPanel>
                        <Label Content="Uygulama Boyutu (Zoom)" Foreground="{DynamicResource TextPrimaryBrush}"/>
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="40"/>
                            </Grid.ColumnDefinitions>
                            <Slider Grid.Column="0" 
                                    Minimum="0.8" Maximum="1.5" 
                                    Value="{Binding UiScale}" 
                                    TickFrequency="0.1" 
                                    IsSnapToTickEnabled="True"/>
                            <TextBlock Grid.Column="1" 
                                       Text="{Binding UiScale, StringFormat=N1}" 
                                       Foreground="{DynamicResource TextPrimaryBrush}"
                                       VerticalAlignment="Center"
                                       HorizontalAlignment="Right"/>
                        </Grid>
                    </StackPanel>
                </GroupBox>

                <!-- Security -->
                <GroupBox Header="Güvenlik" 
                          Foreground="{DynamicResource TextPrimaryBrush}"
                          BorderBrush="{DynamicResource BorderBrush}"
                          Padding="10">
                    <StackPanel>
                        <Label Content="Mevcut Şifre" Foreground="{DynamicResource TextPrimaryBrush}"/>
                        <TextBox Text="{Binding CurrentPassword}"/> 
                        <!-- Note: Binding PasswordBox is tricky in MVVM, using TextBox for simplicity or need attached property -->
                        
                        <Label Content="Yeni Şifre" Foreground="{DynamicResource TextPrimaryBrush}" Margin="0,10,0,0"/>
                        <TextBox Text="{Binding NewPassword}"/>
                        
                        <Button Content="🔑 Şifreyi Değiştir" 
                                Command="{Binding ChangePasswordCommand}"
                                Margin="0,15,0,0"
                                Style="{StaticResource ModernButtonStyle}"/>
                    </StackPanel>
                </GroupBox>
            </StackPanel>
        </ScrollViewer>

        <!-- Footer Actions -->
        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,20,0,0">
            <Button Content="💾 Kaydet" 
                    Command="{Binding SaveCommand}"
                    Style="{StaticResource SuccessButtonStyle}"
                    Width="100"/>
        </StackPanel>
    </Grid>
</Window>
