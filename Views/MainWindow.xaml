<Window x:Class="StudyPlanner.Views.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:models="clr-namespace:StudyPlanner.Models"
        xmlns:helpers="clr-namespace:StudyPlanner.Helpers"
        mc:Ignorable="d"
        Title="Study Planner - Çalışma Planlayıcı" 
        Height="700" Width="1200"
        WindowStartupLocation="CenterScreen"
        Background="{DynamicResource BackgroundBrush}">

    <Window.Resources>
        <!-- Converters -->
        <helpers:PriorityConverter x:Key="PriorityConverter"/>
        
        <!-- Modern Button Style -->
        <Style x:Key="ModernButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="#007ACC"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Padding" Value="15,8"/>
            <Setter Property="Margin" Value="5"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" 
                                CornerRadius="4"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" 
                                            VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#005A9E"/>
                </Trigger>
                <Trigger Property="IsEnabled" Value="False">
                    <Setter Property="Background" Value="#CCCCCC"/>
                    <Setter Property="Foreground" Value="#666666"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <!-- Danger Button Style -->
        <Style x:Key="DangerButtonStyle" TargetType="Button" BasedOn="{StaticResource ModernButtonStyle}">
            <Setter Property="Background" Value="#DC3545"/>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#C82333"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <!-- Success Button Style -->
        <Style x:Key="SuccessButtonStyle" TargetType="Button" BasedOn="{StaticResource ModernButtonStyle}">
            <Setter Property="Background" Value="#28A745"/>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#218838"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <!-- Card Style -->
        <Style x:Key="CardStyle" TargetType="Border">
            <Setter Property="Background" Value="{DynamicResource CardBrush}"/>
            <Setter Property="CornerRadius" Value="8"/>
            <Setter Property="Padding" Value="20"/>
            <Setter Property="Margin" Value="10"/>
            <Setter Property="Effect">
                <Setter.Value>
                    <DropShadowEffect Color="#DDDDDD" BlurRadius="10" ShadowDepth="2" Opacity="0.5"/>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- TextBox Style -->
        <Style TargetType="TextBox">
            <Setter Property="Padding" Value="8"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="BorderBrush" Value="{DynamicResource BorderBrush}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Background" Value="{DynamicResource SurfaceBrush}"/>
            <Setter Property="Foreground" Value="{DynamicResource TextPrimaryBrush}"/>
        </Style>

        <!-- ComboBox Style -->
        <Style TargetType="ComboBox">
            <Setter Property="Padding" Value="8"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Background" Value="{DynamicResource SurfaceBrush}"/>
            <Setter Property="Foreground" Value="{DynamicResource TextPrimaryBrush}"/>
        </Style>

        <!-- Label Style -->
        <Style TargetType="Label">
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Foreground" Value="{DynamicResource TextPrimaryBrush}"/>
        </Style>
    </Window.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/> <!-- Header -->
            <RowDefinition Height="*"/>    <!-- Content -->
        </Grid.RowDefinitions>

        <!-- Header -->
        <Border Grid.Row="0" Background="{DynamicResource PrimaryBrush}" Padding="20">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                
                <StackPanel Grid.Column="0">
                    <TextBlock Text="Çalışma Planlayıcı" 
                             FontSize="24" 
                             FontWeight="Bold" 
                             Foreground="White"/>
                    <TextBlock Text="Çalışmalarınızı planlayın ve takip edin" 
                             FontSize="12" 
                             Foreground="#E0E0E0"
                             Margin="0,5,0,0"/>
                </StackPanel>

                <StackPanel Grid.Column="1" Orientation="Horizontal">
                    <Button Content="📚 PDF Kütüphanem" 
                            Style="{StaticResource ModernButtonStyle}"
                            Background="#4CAF50"
                            Click="PdfLibrary_Click"
                            ToolTip="PDF Kütüphanesi - Kategoriler ve Dökümanlar"/>
                    <Button Content="📊 İstatistikler" 
                            Style="{StaticResource ModernButtonStyle}"
                            Background="#FF9800"
                            Click="Statistics_Click"
                            ToolTip="Çalışma İstatistikleri ve Raporlar"/>
                    <Button Content="📄 Döküman Analiz" 
                            Style="{StaticResource ModernButtonStyle}"
                            Background="#9C27B0"
                            Click="DocumentAnalyzer_Click"
                            ToolTip="Paper Bold AI - Döküman Analizi"/>
                    <Button Content="🌙 Tema" 
                            Style="{StaticResource ModernButtonStyle}"
                            Background="#607D8B"
                            Click="ToggleTheme_Click"
                            ToolTip="Light/Dark Mode"/>
                    <Button Content="💾 Kaydet" 
                            Style="{StaticResource ModernButtonStyle}"
                            Background="#28A745"
                            Command="{Binding SaveCommand}"
                            ToolTip="Dosyaya kaydet"/>
                    <Button Content="📂 Yükle" 
                            Style="{StaticResource ModernButtonStyle}"
                            Background="#FFC107"
                            Command="{Binding LoadCommand}"
                            ToolTip="Dosyadan yükle"/>
                </StackPanel>
            </Grid>
        </Border>

        <!-- Main Content -->
        <Grid Grid.Row="1" Margin="10">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="250"/> <!-- Yan Panel -->
                <ColumnDefinition Width="5"/> <!-- Splitter -->
                <ColumnDefinition Width="1*" MinWidth="300"/>
                <ColumnDefinition Width="2*"/>
            </Grid.ColumnDefinitions>

            <!-- Sol Panel - Hızlı Erişim -->
            <Border Grid.Column="0" Style="{StaticResource CardStyle}">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>

                    <!-- Header -->
                    <StackPanel Grid.Row="0" Margin="0,0,0,15">
                        <TextBlock Text="⚡ Hızlı Erişim" 
                                 FontSize="18" 
                                 FontWeight="Bold"
                                 Foreground="{DynamicResource TextPrimaryBrush}"
                                 Margin="0,0,0,10"/>
                        <Separator/>
                    </StackPanel>

                    <!-- Hızlı Erişim Butonları -->
                    <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
                        <StackPanel>
                            <!-- PDF Kütüphanesi -->
                            <Button Click="PdfLibrary_Click" 
                                    Margin="0,0,0,10"
                                    Padding="15"
                                    HorizontalContentAlignment="Left"
                                    Background="{DynamicResource CardBrush}"
                                    BorderBrush="{DynamicResource BorderBrush}"
                                    BorderThickness="1"
                                    Cursor="Hand">
                                <Button.Style>
                                    <Style TargetType="Button">
                                        <Setter Property="Background" Value="{DynamicResource CardBrush}"/>
                                        <Setter Property="Template">
                                            <Setter.Value>
                                                <ControlTemplate TargetType="Button">
                                                    <Border Background="{TemplateBinding Background}"
                                                            BorderBrush="{TemplateBinding BorderBrush}"
                                                            BorderThickness="{TemplateBinding BorderThickness}"
                                                            CornerRadius="8"
                                                            Padding="{TemplateBinding Padding}">
                                                        <ContentPresenter/>
                                                    </Border>
                                                </ControlTemplate>
                                            </Setter.Value>
                                        </Setter>
                                        <Style.Triggers>
                                            <Trigger Property="IsMouseOver" Value="True">
                                                <Setter Property="Background" Value="{DynamicResource HoverBrush}"/>
                                            </Trigger>
                                        </Style.Triggers>
                                    </Style>
                                </Button.Style>
                                <StackPanel>
                                    <TextBlock Text="📚 PDF Kütüphanem" 
                                             FontSize="16" 
                                             FontWeight="SemiBold"
                                             Foreground="{DynamicResource TextPrimaryBrush}"/>
                                    <TextBlock Text="PDF dökümanlarınızı kategorilere ayırın ve yönetin" 
                                             FontSize="11" 
                                             Foreground="{DynamicResource TextSecondaryBrush}"
                                             TextWrapping="Wrap"
                                             Margin="0,5,0,0"/>
                                </StackPanel>
                            </Button>

                            <!-- İstatistikler -->
                            <Button Click="Statistics_Click" 
                                    Margin="0,0,0,10"
                                    Padding="15"
                                    HorizontalContentAlignment="Left"
                                    Background="{DynamicResource CardBrush}"
                                    BorderBrush="{DynamicResource BorderBrush}"
                                    BorderThickness="1"
                                    Cursor="Hand">
                                <Button.Style>
                                    <Style TargetType="Button">
                                        <Setter Property="Background" Value="{DynamicResource CardBrush}"/>
                                        <Setter Property="Template">
                                            <Setter.Value>
                                                <ControlTemplate TargetType="Button">
                                                    <Border Background="{TemplateBinding Background}"
                                                            BorderBrush="{TemplateBinding BorderBrush}"
                                                            BorderThickness="{TemplateBinding BorderThickness}"
                                                            CornerRadius="8"
                                                            Padding="{TemplateBinding Padding}">
                                                        <ContentPresenter/>
                                                    </Border>
                                                </ControlTemplate>
                                            </Setter.Value>
                                        </Setter>
                                        <Style.Triggers>
                                            <Trigger Property="IsMouseOver" Value="True">
                                                <Setter Property="Background" Value="{DynamicResource HoverBrush}"/>
                                            </Trigger>
                                        </Style.Triggers>
                                    </Style>
                                </Button.Style>
                                <StackPanel>
                                    <TextBlock Text="📊 İstatistikler" 
                                             FontSize="16" 
                                             FontWeight="SemiBold"
                                             Foreground="{DynamicResource TextPrimaryBrush}"/>
                                    <TextBlock Text="Çalışma verilerinizi analiz edin ve raporlayın" 
                                             FontSize="11" 
                                             Foreground="{DynamicResource TextSecondaryBrush}"
                                             TextWrapping="Wrap"
                                             Margin="0,5,0,0"/>
                                </StackPanel>
                            </Button>

                            <!-- Döküman Analiz -->
                            <Button Click="DocumentAnalyzer_Click" 
                                    Margin="0,0,0,10"
                                    Padding="15"
                                    HorizontalContentAlignment="Left"
                                    Background="{DynamicResource CardBrush}"
                                    BorderBrush="{DynamicResource BorderBrush}"
                                    BorderThickness="1"
                                    Cursor="Hand">
                                <Button.Style>
                                    <Style TargetType="Button">
                                        <Setter Property="Background" Value="{DynamicResource CardBrush}"/>
                                        <Setter Property="Template">
                                            <Setter.Value>
                                                <ControlTemplate TargetType="Button">
                                                    <Border Background="{TemplateBinding Background}"
                                                            BorderBrush="{TemplateBinding BorderBrush}"
                                                            BorderThickness="{TemplateBinding BorderThickness}"
                                                            CornerRadius="8"
                                                            Padding="{TemplateBinding Padding}">
                                                        <ContentPresenter/>
                                                    </Border>
                                                </ControlTemplate>
                                            </Setter.Value>
                                        </Setter>
                                        <Style.Triggers>
                                            <Trigger Property="IsMouseOver" Value="True">
                                                <Setter Property="Background" Value="{DynamicResource HoverBrush}"/>
                                            </Trigger>
                                        </Style.Triggers>
                                    </Style>
                                </Button.Style>
                                <StackPanel>
                                    <TextBlock Text="🤖 AI Döküman Analiz" 
                                             FontSize="16" 
                                             FontWeight="SemiBold"
                                             Foreground="{DynamicResource TextPrimaryBrush}"/>
                                    <TextBlock Text="Paper Bold AI ile akademik makaleleri analiz edin" 
                                             FontSize="11" 
                                             Foreground="{DynamicResource TextSecondaryBrush}"
                                             TextWrapping="Wrap"
                                             Margin="0,5,0,0"/>
                                </StackPanel>
                            </Button>

                            <!-- Tema Değiştirici -->
                            <Button Click="ToggleTheme_Click" 
                                    Margin="0,0,0,10"
                                    Padding="15"
                                    HorizontalContentAlignment="Left"
                                    Background="{DynamicResource CardBrush}"
                                    BorderBrush="{DynamicResource BorderBrush}"
                                    BorderThickness="1"
                                    Cursor="Hand">
                                <Button.Style>
                                    <Style TargetType="Button">
                                        <Setter Property="Background" Value="{DynamicResource CardBrush}"/>
                                        <Setter Property="Template">
                                            <Setter.Value>
                                                <ControlTemplate TargetType="Button">
                                                    <Border Background="{TemplateBinding Background}"
                                                            BorderBrush="{TemplateBinding BorderBrush}"
                                                            BorderThickness="{TemplateBinding BorderThickness}"
                                                            CornerRadius="8"
                                                            Padding="{TemplateBinding Padding}">
                                                        <ContentPresenter/>
                                                    </Border>
                                                </ControlTemplate>
                                            </Setter.Value>
                                        </Setter>
                                        <Style.Triggers>
                                            <Trigger Property="IsMouseOver" Value="True">
                                                <Setter Property="Background" Value="{DynamicResource HoverBrush}"/>
                                            </Trigger>
                                        </Style.Triggers>
                                    </Style>
                                </Button.Style>
                                <StackPanel>
                                    <TextBlock Text="🌙 Tema Değiştir" 
                                             FontSize="16" 
                                             FontWeight="SemiBold"
                                             Foreground="{DynamicResource TextPrimaryBrush}"/>
                                    <TextBlock Text="Light / Dark Mode geçişi" 
                                             FontSize="11" 
                                             Foreground="{DynamicResource TextSecondaryBrush}"
                                             TextWrapping="Wrap"
                                             Margin="0,5,0,0"/>
                                </StackPanel>
                            </Button>

                            <Separator Margin="0,10"/>

                            <!-- Info -->
                            <Border Background="{DynamicResource HoverBrush}"
                                    CornerRadius="8"
                                    Padding="12"
                                    Margin="0,10,0,0">
                                <StackPanel>
                                    <TextBlock Text="💡 İpucu" 
                                             FontSize="13" 
                                             FontWeight="SemiBold"
                                             Foreground="{DynamicResource TextPrimaryBrush}"/>
                                    <TextBlock Text="PDF Kütüphanesi'nde dökümanlarınızı kategorilere ayırabilir ve tek tıkla açabilirsiniz!" 
                                             FontSize="11" 
                                             Foreground="{DynamicResource TextSecondaryBrush}"
                                             TextWrapping="Wrap"
                                             Margin="0,5,0,0"/>
                                </StackPanel>
                            </Border>
                        </StackPanel>
                    </ScrollViewer>
                </Grid>
            </Border>

            <!-- Splitter -->
            <GridSplitter Grid.Column="1" 
                         Width="5" 
                         HorizontalAlignment="Stretch"
                         Background="Transparent"/>

            <!-- Orta Panel - Form -->
            <Border Grid.Column="2" Style="{StaticResource CardStyle}">
                <StackPanel>
                    <TextBlock Text="📝 Yeni Görev Ekle" 
                             FontSize="18" 
                             FontWeight="Bold"
                             Foreground="{DynamicResource TextPrimaryBrush}"
                             Margin="0,0,0,20"/>

                    <!-- Subject -->
                    <Label Content="Ders / Konu:"/>
                    <TextBox Text="{Binding CurrentItem.Subject, UpdateSourceTrigger=PropertyChanged}" 
                           Margin="0,0,0,10"/>

                    <!-- Category -->
                    <Label Content="Kategori:"/>
                    <TextBox Text="{Binding CurrentItem.Category, UpdateSourceTrigger=PropertyChanged}" 
                           Margin="0,0,0,10"/>

                    <!-- Date -->
                    <Label Content="Tarih / Saat:"/>
                    <DatePicker SelectedDate="{Binding CurrentItem.Date}" 
                              Margin="0,0,0,10"/>

                    <!-- Duration -->
                    <Label Content="Süre (Dakika):"/>
                    <TextBox Text="{Binding CurrentItem.DurationMinutes, UpdateSourceTrigger=PropertyChanged}" 
                           Margin="0,0,0,10"/>

                    <!-- Priority -->
                    <Label Content="Öncelik:"/>
                    <ComboBox SelectedIndex="{Binding CurrentItem.PriorityIndex}" 
                            Margin="0,0,0,10">
                        <ComboBoxItem Content="Düşük" Tag="Low"/>
                        <ComboBoxItem Content="Orta" Tag="Medium"/>
                        <ComboBoxItem Content="Yüksek" Tag="High"/>
                        <ComboBoxItem Content="Kritik" Tag="Critical"/>
                    </ComboBox>

                    <!-- Notes -->
                    <Label Content="Notlar:"/>
                    <TextBox Text="{Binding CurrentItem.Notes, UpdateSourceTrigger=PropertyChanged}" 
                           Height="80" 
                           TextWrapping="Wrap" 
                           AcceptsReturn="True" 
                           VerticalScrollBarVisibility="Auto"
                           Margin="0,0,0,20"/>

                    <!-- Buttons -->
                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                        <Button Content="➕ Plana Ekle" 
                              Style="{StaticResource SuccessButtonStyle}"
                              Command="{Binding AddCommand}"
                              Width="140"
                              ToolTip="Çalışma planını listeye ekle"/>
                        <Button Content="🧹 Temizle" 
                              Style="{StaticResource ModernButtonStyle}"
                              Command="{Binding NewCommand}"
                              Width="120"
                              ToolTip="Formu temizle ve yeni plan için hazırla"/>
                    </StackPanel>
                </StackPanel>
            </Border>

            <!-- Sağ Panel - List -->
            <Border Grid.Column="3" Style="{StaticResource CardStyle}">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>

                    <!-- Search and Filter -->
                    <Grid Grid.Row="0" Margin="0,0,0,15">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>

                        <TextBox Grid.Column="0" 
                               Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged}"
                               Padding="10"
                               FontSize="14"
                               VerticalContentAlignment="Center">
                            <TextBox.Style>
                                <Style TargetType="TextBox">
                                    <Style.Triggers>
                                        <Trigger Property="Text" Value="">
                                            <Setter Property="Background">
                                                <Setter.Value>
                                                    <VisualBrush Stretch="None" AlignmentX="Left">
                                                        <VisualBrush.Visual>
                                                            <TextBlock Text="🔍 Ara..." 
                                                                     Foreground="#AAAAAA" 
                                                                     Margin="10,0,0,0"/>
                                                        </VisualBrush.Visual>
                                                    </VisualBrush>
                                                </Setter.Value>
                                            </Setter>
                                        </Trigger>
                                    </Style.Triggers>
                                </Style>
                            </TextBox.Style>
                        </TextBox>

                        <CheckBox Grid.Column="1" 
                                Content="Sadece Tamamlananlar" 
                                IsChecked="{Binding ShowCompletedOnly}"
                                VerticalAlignment="Center"
                                Margin="10,0,0,0"/>
                    </Grid>

                    <!-- Data Grid -->
                    <DataGrid Grid.Row="1" 
                            ItemsSource="{Binding StudyPlans}"
                            SelectedItem="{Binding SelectedItem}"
                            AutoGenerateColumns="False"
                            CanUserAddRows="False"
                            CanUserDeleteRows="False"
                            SelectionMode="Single"
                            GridLinesVisibility="None"
                            HeadersVisibility="Column"
                            Background="{DynamicResource SurfaceBrush}"
                            Foreground="{DynamicResource TextPrimaryBrush}"
                            RowBackground="{DynamicResource CardBrush}"
                            AlternatingRowBackground="{DynamicResource HoverBrush}"
                            BorderThickness="0">
                        <DataGrid.ColumnHeaderStyle>
                            <Style TargetType="DataGridColumnHeader">
                                <Setter Property="Background" Value="{DynamicResource PrimaryBrush}"/>
                                <Setter Property="Foreground" Value="White"/>
                                <Setter Property="Padding" Value="10,8"/>
                                <Setter Property="FontWeight" Value="SemiBold"/>
                                <Setter Property="BorderThickness" Value="0,0,1,0"/>
                                <Setter Property="BorderBrush" Value="{DynamicResource BorderBrush}"/>
                            </Style>
                        </DataGrid.ColumnHeaderStyle>
                        <DataGrid.CellStyle>
                            <Style TargetType="DataGridCell">
                                <Setter Property="BorderThickness" Value="0"/>
                                <Setter Property="Padding" Value="10,5"/>
                                <Setter Property="Background" Value="Transparent"/>
                                <Setter Property="Foreground" Value="{DynamicResource TextPrimaryBrush}"/>
                                <Style.Triggers>
                                    <Trigger Property="IsSelected" Value="True">
                                        <Setter Property="Background" Value="{DynamicResource PrimaryBrush}"/>
                                        <Setter Property="Foreground" Value="White"/>
                                    </Trigger>
                                </Style.Triggers>
                            </Style>
                        </DataGrid.CellStyle>
                        <DataGrid.Columns>
                            <DataGridCheckBoxColumn Header="✓" 
                                                  Binding="{Binding IsCompleted, UpdateSourceTrigger=PropertyChanged}" 
                                                  Width="40"/>
                            <DataGridTextColumn Header="Ders/Konu" 
                                              Binding="{Binding Subject}" 
                                              Width="*"/>
                            <DataGridTextColumn Header="Kategori" 
                                              Binding="{Binding Category}" 
                                              Width="100"/>
                            <DataGridTextColumn Header="Tarih" 
                                              Binding="{Binding DateDisplay}" 
                                              Width="150"/>
                            <DataGridTextColumn Header="Süre" 
                                              Binding="{Binding DurationDisplay}" 
                                              Width="100"/>
                            <DataGridTextColumn Header="Öncelik" 
                                              Binding="{Binding PriorityDisplay}" 
                                              Width="100"/>
                        </DataGrid.Columns>
                    </DataGrid>

                    <!-- Action Buttons -->
                    <StackPanel Grid.Row="2" 
                              Orientation="Horizontal" 
                              HorizontalAlignment="Right" 
                              Margin="0,15,0,0">
                        <Button Content="✏️ Güncelle" 
                              Style="{StaticResource ModernButtonStyle}"
                              Command="{Binding UpdateCommand}"/>
                        <Button Content="❌ Sil" 
                              Style="{StaticResource DangerButtonStyle}"
                              Command="{Binding DeleteCommand}"/>
                        <Button Content="✓ Tamamlandı" 
                              Style="{StaticResource SuccessButtonStyle}"
                              Command="{Binding ToggleCompleteCommand}"/>
                    </StackPanel>
                </Grid>
            </Border>
        </Grid>
    </Grid>
</Window>

