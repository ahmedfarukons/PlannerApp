<Window x:Class="StudyPlanner.Views.FocusZoneWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="🎯 Focus Zone"
        Height="650" Width="900"
        WindowStartupLocation="CenterScreen"
        ResizeMode="CanResize"
        Background="{DynamicResource BackgroundBrush}">

    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <Border Grid.Row="0" Background="{DynamicResource CardBrush}" CornerRadius="14" Padding="18" Margin="0,0,0,18">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <StackPanel Grid.Column="0">
                    <TextBlock Text="🎯 Focus Zone"
                               FontSize="26"
                               FontWeight="Bold"
                               Foreground="{DynamicResource TextPrimaryBrush}"/>
                    <TextBlock Text="Pomodoro / derin odak modu"
                               FontSize="13"
                               Foreground="{DynamicResource TextSecondaryBrush}"
                               Margin="0,6,0,0"/>
                </StackPanel>
                <StackPanel Grid.Column="1" VerticalAlignment="Center" Orientation="Horizontal">
                    <Button Content="25:00" Command="{Binding SetPreset25Command}" Margin="0,0,10,0" Padding="14,8"/>
                    <Button Content="50:00" Command="{Binding SetPreset50Command}" Margin="0,0,10,0" Padding="14,8"/>
                    <Button Content="90:00" Command="{Binding SetPreset90Command}" Padding="14,8"/>
                </StackPanel>
            </Grid>
        </Border>

        <!-- Body -->
        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="2*"/>
                <ColumnDefinition x:Name="SettingsColumn" Width="1*"/>
            </Grid.ColumnDefinitions>

            <!-- Big timer -->
            <Border Grid.Column="0" Background="{DynamicResource CardBrush}" CornerRadius="14" Padding="22" Margin="0,0,18,0">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>

                    <TextBlock Grid.Row="0"
                               Text="Kalan Süre"
                               FontSize="14"
                               Foreground="{DynamicResource TextSecondaryBrush}"/>

                    <Viewbox Grid.Row="1" Margin="0,14,0,10" MaxHeight="150">
                        <TextBlock Text="{Binding RemainingDisplay}"
                                   FontWeight="Bold"
                                   Foreground="{DynamicResource TextPrimaryBrush}"/>
                    </Viewbox>

                    <ProgressBar Grid.Row="2"
                                 Height="18"
                                 Minimum="0"
                                 Maximum="100"
                                 Value="{Binding ProgressPercent, Mode=OneWay}"
                                 Margin="0,10,0,0"/>

                    <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,18,0,0">
                        <Button Content="{Binding StartPauseLabel}"
                                Command="{Binding StartPauseCommand}"
                                Padding="18,10"
                                Margin="0,0,12,0"/>
                        <Button Content="⟲ Sıfırla"
                                Command="{Binding ResetCommand}"
                                Padding="18,10"/>
                    </StackPanel>
                </Grid>
            </Border>

            <!-- Settings -->
            <Border x:Name="SettingsPanel" Grid.Column="1" Background="{DynamicResource CardBrush}" CornerRadius="14" Padding="18">
                <StackPanel>
                    <Grid Margin="0,0,0,8">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>
                        <TextBlock Grid.Column="0" Text="Ayarlar" FontSize="16" FontWeight="SemiBold" Foreground="{DynamicResource TextPrimaryBrush}"/>
                        <Button Grid.Column="1"
                                Content="▸"
                                Width="34"
                                Height="28"
                                Padding="0"
                                Margin="8,0,0,0"
                                Command="{Binding ToggleSettingsCommand}"
                                ToolTip="Ayarları Aç/Kapat"/>
                    </Grid>
                    <TextBlock Text="Süre (dakika)" FontSize="13" Foreground="{DynamicResource TextSecondaryBrush}" Margin="0,14,0,6"/>

                    <Slider Minimum="1"
                            Maximum="180"
                            Value="{Binding DurationMinutes}"
                            TickFrequency="5"
                            IsSnapToTickEnabled="True"/>
                    <TextBlock Text="{Binding DurationMinutes, StringFormat='{}{0} dk'}"
                               FontSize="18"
                               FontWeight="Bold"
                               Foreground="{DynamicResource TextPrimaryBrush}"
                               Margin="0,8,0,0"/>

                    <Border Background="{DynamicResource HoverBrush}" CornerRadius="12" Padding="12" Margin="0,18,0,0">
                        <TextBlock Text="İpucu: Süreyi ayarla → Başlat. Süre dolunca bildirim çıkar."
                                   TextWrapping="Wrap"
                                   Foreground="{DynamicResource TextPrimaryBrush}"
                                   FontSize="13"/>
                    </Border>
                </StackPanel>
            </Border>
        </Grid>
    </Grid>
</Window>


